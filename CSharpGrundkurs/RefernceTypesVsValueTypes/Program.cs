using System;

namespace RefernceTypesVsValueTypes
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Wertetypen sind -> bool, int, long, byte, decimal, double, float, struct
            //Referenztypen -> string, class, interface, record (intern eine Klasse), 
        }
    }
}
